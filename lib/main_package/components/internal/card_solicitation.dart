import 'package:flutter/material.dart';

class CardSolicitation extends StatelessWidget {
  const CardSolicitation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
