import 'package:flutter/material.dart';

class MeetDetailForm extends StatelessWidget {
  const MeetDetailForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
